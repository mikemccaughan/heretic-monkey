{
  "name": "heretic-server",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "build": "npx npm-check-updates -u && npm install --force && tsc --build",
    "start": "npx npm-check-updates -u && npm install --force && node ../dist/server/index.js"
  },
  "private": true,
  "dependencies": {
    "body-parser": "^2.2.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6"
  }
}
